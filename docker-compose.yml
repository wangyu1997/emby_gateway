services:
  emby302:
    image: ${DOCKERHUB_USERNAME:-your-username}/emby302:latest
    restart: unless-stopped
    ports:
      - "8095:8095"
      - "8098:8098"
    volumes:
      - ./data:/data
    environment:
      - TZ=Asia/Shanghai
